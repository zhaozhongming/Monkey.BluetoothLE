<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 x:Class="BLEExplorer.Pages.CharacteristicDetail"
			 Title="Characteristic">

    <Grid x:Name="MainGrid" Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        
        <StackLayout Grid.Row="0" Grid.Column="0">
            <Button x:Name="btnSendAny" Text="Any Key" />
            <Button x:Name="btnListData" Text="List Data" />
            <Button x:Name="btnReset" Text="Reset" />
        </StackLayout>
        <StackLayout Grid.Row="0" Grid.Column="1">
            <Button x:Name="btnStop" Text="Stop Reading" />
            <Button x:Name="btnStart" Text="Start Reading" />
            <Button x:Name="btnExit" Text="Exit" />
        </StackLayout>
        <ScrollView Orientation="Vertical"  Grid.Row="1" Grid.ColumnSpan="2">
            <Label x:Name="entryReceived" />
        </ScrollView>
    </Grid>
</ContentPage>
